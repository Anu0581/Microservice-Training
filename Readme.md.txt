In Each microservice where dockerfle is there, for Docker Build we need to run below command

docker build -t microservice/name .

After that can go to docker terminal and then run below command where docker-compose file is there
docker-compose up
